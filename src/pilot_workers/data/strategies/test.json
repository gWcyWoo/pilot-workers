{
  "layers": [
    {"name": "suite", "focus": "The project's standard test command — whatever `CLAUDE.md`, `README.md`, `package.json`, `Makefile` or the CI config names as the way to run the tests. Run it once, in full."}
  ]
}
