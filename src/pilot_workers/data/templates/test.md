<!-- pilot-workers task · test mode. Replace every commented placeholder below. -->

# Commands

<!--PILOT_FILL Exact commands to run and the directory to run them in, one per line. -->

# Known Pre-existing Failures

<!--PILOT_FILL Known pre-existing failures (to avoid false positives reported as new issues); write none if there are none. -->
