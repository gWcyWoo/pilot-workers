<!-- pilot-workers task · discuss mode (read-only, no fixes). Replace every commented placeholder below. -->

<!-- Most callers do not need this template: `pw9 discuss --question "..."`
     generates the task automatically. Fill this in only when the question
     needs more framing than one flag carries. -->

# Question

<!--PILOT_FILL The decision to be made, stated so a model that has never seen this project can answer it. If there are named options, name them; if it is open-ended, say so. -->

# Codebase

<!--PILOT_FILL Where to look: the files, module or flow the question touches. The worker must ground claims about current behavior in file:line. -->

# Constraints already settled

<!--PILOT_FILL What is NOT up for debate — decisions already made, requirements that cannot move, approaches already ruled out and why. Without this, models argue for options you have already rejected. -->

# What a useful answer looks like

<!--PILOT_FILL The form of answer that would actually help: a pick between the named options, a recommended structure, a risk assessment. Also say what you will do with the answer, so the model can calibrate depth. -->
