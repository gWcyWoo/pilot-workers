<!-- pilot-workers task · review mode (read-only, no fixes). Replace every commented placeholder below. -->

# Review Target

<!--PILOT_FILL What to review: which files / which diff / which change. Include background context. -->

# Coverage Map

<!--PILOT_FILL The dimensions × surfaces to cover, enumerated by YOU, not left to the worker to invent. What gets found is what was asked: ground you do not name here is what the next round discovers. Dimensions: correctness, credential exposure, data/work loss, concurrency, layering, contracts between parts, failure/recovery, docs-vs-code, test protection. Surfaces: the exact files or subsystems. Say which pairs matter and which are out of scope, so "not examined" is a decision rather than an omission. -->

# Directions

<!--PILOT_FILL Review focus areas; list 3-5 specific checkpoints for each. These are where to START, not a fence: an out-of-focus high beats an in-focus low. -->

# Already Fixed — do not re-report

<!--PILOT_FILL Defects already fixed, one line each, so the round is not spent re-finding them. State plainly that this is a de-duplication aid, not a protected list: if the worker examines one and believes it is NOT actually fixed, that is a finding. Write "none" for a first review. -->
