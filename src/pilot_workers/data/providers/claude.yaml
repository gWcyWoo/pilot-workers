# Claude via Anthropic's official API. Requires an Anthropic API key.
# `declare: false` — OpenCode's own registry already carries the anthropic
# provider (endpoint, npm package, full model catalogue), so pw9 must not
# shadow it with a thinner copy. No base_url or npm needed here.
#
# A Claude Pro/Max SUBSCRIPTION cannot be used: Anthropic prohibits driving
# it from third-party clients, and OpenCode removed that path in 1.3.0.
# Docs: https://docs.anthropic.com/

key: claude
provider_id: anthropic
model_id: claude-opus-4-8
display_name: Claude Opus 4.8 Worker
context_tokens: 1000000
output_tokens: 128000
declare: false
strengths: long-context reasoning, contract-grade precision; coding, review, exploration
suitable_modes: code, review, explore, discuss
notes: requires an Anthropic API key (console.anthropic.com), not a Claude Pro/Max subscription
